{% extends "base.html.twig" %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col">
            <ul class="list-group">
                {% for organization in organizations %}
                <li class="list-group-item">
                    <a href="{{path("organization_surveys", {id: organization.id})}}">{{ organization.name }}</a>
                </li>    
                {% endfor %}
            </ul>
        </div>
    </div>
</div>
{% endblock %}