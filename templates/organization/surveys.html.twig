{% extends "base.html.twig" %}

{% block body %}
<div class="container">
	<div class="row py-4">
		<div class="col">
			{% for survey in open_surveys %}
			<ul class="list-group">
				<li class="list-group-item">
					<a href="{{ path("surveys_response", {id: survey.id}) }}">
						<span class="badge badge-{{ survey.status == 2 ? 'secondary' : 'primary' }}">{{ survey.status == 2 ? 'closed' : 'open' }}</span>
						{{ survey.surveyTemplate.name }}
					</a>
				</li>
			</ul>
			{% endfor %}
		</div>
	</div>
</div>
{% endblock %}